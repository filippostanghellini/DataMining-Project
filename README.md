# Dataset download (glf)

```markdown
# macOS
brew install git-lfs

# Ubuntu/Debian
sudo apt install git-lfs

# Initialize Git LFS
git lfs install
```
Dataset: https://snap.stanford.edu/data/amazon-meta.html